{
  "name": "usdt-transfer-agent",
  "version": "0.0.1",
  "description": "Forta agent that detects when USDT tokens are transferred",
  "scripts": {
    "start": "npm run start:dev",
    "start:dev": "nodemon --watch src --watch forta.config.json -e js,json --exec 'forta-agent run'",
    "start:prod": "forta-agent run --prod",
    "test": "jest"
  },
  "dependencies": {
    "forta-agent": "^0.0.23"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.24.2",
    "jest": "^27.1.1",
    "nodemon": "^2.0.12"
  }
}
